function createTagsFromSubject() {
  var threads=GmailApp.getInboxThreads();
  for(var i=0;i<threads.length;i++){
  var subject=threads[i].getMessages()[0].getSubject()
    Logger.log(subject)
  }
}

function createTags(subject)
{
GmailApp.
}
